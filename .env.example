# ============================================================================
# LLM Search Analysis - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# Usage: cp .env.example .env
#
# REQUIRED: API keys for at least one LLM provider
# OPTIONAL: Other configuration values (defaults are provided)
# ============================================================================

# ----------------------------------------------------------------------------
# Application Settings
# ----------------------------------------------------------------------------
APP_NAME=LLM Search Analysis API
VERSION=1.0.0
DEBUG=false

# ----------------------------------------------------------------------------
# LLM Provider API Keys (REQUIRED - at least one)
# ----------------------------------------------------------------------------
# Get your API keys from:
# - OpenAI: https://platform.openai.com/api-keys
# - Google: https://aistudio.google.com/app/apikey
# - Anthropic: https://console.anthropic.com/settings/keys

OPENAI_API_KEY=your_openai_key_here
GOOGLE_API_KEY=your_google_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here

# ----------------------------------------------------------------------------
# ChatGPT Account (Optional - for network capture with search)
# ----------------------------------------------------------------------------
# Uncomment and fill in if you want to use ChatGPT network capture mode
# CHATGPT_EMAIL=your_email@example.com
# CHATGPT_PASSWORD=your_password

# ----------------------------------------------------------------------------
# Network Capture Settings (Optional)
# ----------------------------------------------------------------------------
# Used for capturing network logs from ChatGPT and other LLM web interfaces
# Set BROWSER_HEADLESS=false to see the browser during capture

BROWSER_HEADLESS=true
CHATGPT_SESSION_FILE=./data/chatgpt_session.json
NETWORK_LOGS_DIR=./data/network_logs

# ----------------------------------------------------------------------------
# Database Settings (Optional)
# ----------------------------------------------------------------------------
# For local development (non-Docker):
# DATABASE_URL=sqlite:///data/llm_search.db
#
# For Docker (automatically configured in docker-compose.yml):
# DATABASE_URL=sqlite:///./data/llm_search.db

# ----------------------------------------------------------------------------
# Logging
# ----------------------------------------------------------------------------
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# Docker-Specific Settings (Auto-configured in docker-compose.yml)
# ============================================================================
# These are automatically set by docker-compose.yml and don't need to be
# changed unless you're customizing the deployment:
#
# - HOST=0.0.0.0 (listen on all interfaces)
# - PORT=8000 (FastAPI backend port)
# - API_BASE_URL=http://api:8000 (frontend → backend connection)
# - CORS_ORIGINS=http://localhost:8501,http://frontend:8501 (allow frontend)
# ============================================================================
