# ============================================================================
# LLM Search Analysis - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# Then edit .env with your API keys and configuration.
#
# DEPLOYMENT MODES:
#   1. Docker Compose (recommended): `docker compose up -d`
#   2. Local Development: Run backend and frontend separately
# ============================================================================

# ============================================================================
# REQUIRED: LLM Provider API Keys
# ============================================================================
# You need at least ONE API key to use the application.
# Get your keys from:
#   - OpenAI: https://platform.openai.com/api-keys
#   - Google: https://aistudio.google.com/app/apikey
#   - Anthropic: https://console.anthropic.com/settings/keys

OPENAI_API_KEY=
GOOGLE_API_KEY=
ANTHROPIC_API_KEY=

# ============================================================================
# OPTIONAL: ChatGPT Network Capture Mode
# ============================================================================
# Required ONLY if you want to use network capture mode with ChatGPT.
# This captures browser network traffic for deeper analysis.
# Leave blank if you're only using official APIs.

CHATGPT_EMAIL=
CHATGPT_PASSWORD=

# ============================================================================
# Application Settings
# ============================================================================
# Basic application metadata

APP_NAME=LLM Search Analysis API
VERSION=1.0.0
DEBUG=false
LOG_LEVEL=INFO

# ============================================================================
# Deployment Configuration
# ============================================================================
# Different settings needed for Docker vs Local Development

# --- Docker Compose Deployment (default) ---
# These are automatically set in docker-compose.yml:
#   - HOST=0.0.0.0
#   - PORT=8000
#   - DATABASE_URL=sqlite:///./data/llm_search.db
#   - API_BASE_URL=http://api:8000
#   - CORS_ORIGINS=["http://localhost:8501","http://frontend:8501"]

# --- Local Development (without Docker) ---
# Uncomment these if running backend and frontend manually:
# HOST=127.0.0.1
# PORT=8000
# DATABASE_URL=sqlite:///../data/llm_search.db
# API_BASE_URL=http://localhost:8000
# CORS_ORIGINS=["http://localhost:8501","http://localhost:3000"]

# ============================================================================
# Network Capture Settings (Advanced)
# ============================================================================
# Configuration for browser automation and network capture

BROWSER_HEADLESS=true
CHATGPT_SESSION_FILE=./data/chatgpt_session.json
NETWORK_LOGS_DIR=./data/network_logs

# ============================================================================
# Test Controls
# ============================================================================
# Set RUN_E2E=1 when you explicitly want to run the live provider
# persistence tests. Those tests hit the real OpenAI/Google/Anthropic
# APIs and require valid keys plus internet access. Leave at 0 (or unset)
# for normal local/CI runs.
RUN_E2E=0

# ============================================================================
# Notes
# ============================================================================
# 1. For Docker: Just set API keys and run `docker compose up -d`
# 2. For Local Dev: Uncomment the "Local Development" section above
# 3. Database persists in ./backend/data/llm_search.db (Docker)
#    or ./data/llm_search.db (Local)
# 4. Logs use LOG_LEVEL (DEBUG for development, INFO for production)
# ============================================================================
